package com.revature;

public class Calculator {
	
	/*
	 * We'll create a calculator that accepts a string
	 * input . It can accept 0,1, or 2 numbers separated by a comma.
	 * If we provide 9 numbers (empty string) we want our calculator to return 0)
	 * If we provide 1 we want it to return that number
	 * If we provide 2 we want it to return the sum of them
	 * If we provide an invalid or null input, we want it to return -1
	 */

	public static int add(String values) {
		return 0;
	}
}
